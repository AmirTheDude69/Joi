# Joi web/PWA

This Next.js application is the standalone Joi client. It streams from the shared agent service through a server-only proxy and never exposes the agent credential to browser JavaScript.

Run from the repository root with `pnpm dev:web`. See the root `README.md` and `docs/architecture.md` for setup and design details.
